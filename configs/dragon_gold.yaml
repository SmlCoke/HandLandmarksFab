schema_version: finetune_gold_v1
workspace_root: ${HAND_WORK_ROOT}/finetune/${HAND_FINETUNE_ID}
gold_repository_root: ${HAND_GOLD_ROOT}

hand_roi:
  output_width: 256
  output_height: 256
  scale_x: 1.8
  scale_y: 1.8
  shift_x: 0.0
  shift_y: -0.1

dragon:
  images_dir: images
  hand_annotations: annotations_hand.txt
  palm_annotations: annotations_palm.txt
  readme: README.md
  overlay_count: 64
